import LoadingStyledContainer from '@/components/ui/container/loading'

const TableSkeleton = () => {
	return <LoadingStyledContainer height={320} sx={{ marginTop: 6 }} />
}

export default TableSkeleton
